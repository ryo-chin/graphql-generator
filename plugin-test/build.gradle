buildscript {
    dependencies {
        classpath files('graphql-generator-1.0-SNAPSHOT.jar')
    }
}

apply plugin: 'com.github.ryo-chin.generate'

graphql_generator {
    inputPath = 'src/test/resources/graphql/schema.graphql'
    outputPath = 'tmp/autogen'
    idType = 'Long'
}
